<div class="col-md-4">
    <ul class="list-group">
        {{#each burgers}}
        <li class="list-group-item d-flex justify-content-between align-items-center">
            {{this.name}}
            <button type="button" class="btn btn-warning">Devour It!</button>
        </li>
        {{/each}}
    </ul>
</div>
<div class="col-md-4"></div>
<div class="col-md-4">
    <ul class="list-group">
        {{#each burgers}}
        <li class="list-group-item d-flex justify-content-between align-items-center">
            {{this.name}}
            <span class="badge badge-primary badge-pill">Devoured!</span>
        </li>
        {{/each}}
    </ul>
</div>